'use strict';;_049‍.x([["Stats",()=>Stats],["charObj",()=>charObj],["duel",()=>duel]]);

/*
	the purpose of this module will be to: 
	TODO - handle the character creation process
*/

class Stats {
	constructor(obj){
		this.health = obj.health;
		this.damage = obj.damage;
		this.speed = obj.speed;
	}
}
class charObj extends Stats {
	constructor(obj){
		super(obj);
		this.name = obj.name;
	}
}

let duel = function(char1, char2) {
	// let timer = setInterval(,);
	let char1att = setInterval(
		function(){
			console.log(`${char1.name} just attacked!`);
			// char2.health -= char1.damage;
	}, (char1.speed*1000));
	let char2att = setInterval(
		function(){
			console.log(`${char2.name} just attacked!`)
			// char1.health -= char2.damage;
	}, (char2.speed*1000));

	// clearInterval(char1att);
}

// let obj = [1000,50,1];
// let char1 = new charObj({'name':'char1','health':obj[0],'damage':obj[1],'speed':obj[2]});
// let char2 = new charObj({'name':'char2','health':obj[0],'damage':obj[1],'speed':obj[2]});

// duel(char1,char2);



//TODO: NEED TO FIX EXPORT STATEMENT
// module.exports = duel;